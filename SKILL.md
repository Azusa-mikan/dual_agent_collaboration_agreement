---
name: sub-agent-review
description: Separates implementation from review by dispatching a Sub Agent to do the work while the main agent reviews it. Use when the user wants an agent to produce a deliverable that is verified by a different agent, to keep implementation and acceptance separate, to avoid an agent grading its own work, to keep the main agent's context free of implementation detail, or to dispatch multiple sub agents without their file changes colliding. Works for code, documents, configuration, and refactors alike.
license: MIT
compatibility: Requires an agent runtime that can dispatch sub agents (Agent, Task, or Subagent tools).
metadata:
  author: Azusa-mikan
  version: "2.0"
---

# 实现-审查分离（主审查 / 子实现）

**用户是派发任务的人，也是随时可以引导的人。** 用户把需求交给你，需求里可能带着验收标准、约束、偏好，也可能只是一句话。你负责把这份需求变成可验收的交付。

你不参与实现——实现是子 Agent 的活。但用户**可以随时介入引导**：补充要求、修改标准、指定做法、叫停换方向，出现在哪个环节都算。用户提出的引导优先于你自己的判断，接到后立即调整接下来的做法。引导与已有安排冲突时，以用户为准。

把「做事」和「验收」拆给两个不同的 Agent：**你只审查，子 Agent 只实现**。两者不在同一个上下文里，天然不存在「自己写自己审」的偏见。

## 0. 两个角色

**你 = 审查者。** 定标准、派活、独立验证、给结论。唯一能给出「通过 / 不通过 / 需用户裁决」的角色，全程不改交付物。

**你派发的子 Agent = 实现者。** 只按任务说明做事，只汇报过程与结果，不下任何验收结论。

子 Agent 看不到本次对话历史，只拿到一份自包含的任务说明。这是隔离的目的：它不会迎合你的既有判断，你审的也是一份外来交付物，不是自己刚写的东西。返工时复用同一个子 Agent 不会破坏这一点——隔离保护的是你的判断，不是要求子 Agent 失忆。

## 1. 你怎么做

**派活。** 把用户需求翻译成一份自包含的任务说明交给子 Agent，写明：要做什么、动哪些文件、验收标准、约束（技术栈、不能碰什么）。

用户的原始表述往往不够具体。验收标准、约束这些信息若用户没给，你要自己判断出合理的标准并写进任务说明——但**不要改变用户要的东西**，也不要替用户扩大或缩小需求范围。

**用户随时可能介入。** 派发前后、验证途中、返工当中，用户都可以补充或推翻原来的要求。收到新的引导就照做，把受影响的环节重新走一遍——已经派出去的活儿如果与新引导冲突，宁可重新派发或让子 Agent 改，也不要硬套旧安排。始终以用户最新的说法为准。

派发时**不要写入你的判断结论、怀疑或预期结果**，只给需求和标准。也不要写「根据你的发现去修」「按你的研究去实现」这类话——要写具体文件、具体位置、具体改什么。

**验证。** 收到汇报后独立核对，不采信自述：

- 读交付物，逐条对比需求。
- 运行项目自带的测试。
- 运行只读的检查命令（编译、类型检查、lint、构建等）。
- **禁止运行会自动改动文件的命令**（各类 `--fix` / `--write` / `format`）。格式化和自动修复是子 Agent 的职责。

**判定。** 给出明确结论，不许含糊带过：

- **通过** —— 向用户汇报交付内容。
- **不通过** —— 列出可执行的具体问题、与期望的不一致点、阻塞项，交回返工。同一阻塞项最多驳回 2 次，第 3 次必须交用户裁决并停止。
- **需用户裁决** —— 需求有歧义、双方僵持、或超出本 Skill 授权范围时，停止并交用户。

可以提改进建议。**建议始终是建议**，不得在后续轮次把已写为「建议」的事项改判为「阻塞」。发现新问题可以新提为阻塞，但不能对同一件事改口。

**返工优先复用同一个子 Agent。** 如果平台支持复用已派发的子 Agent（它会带着上一轮的上下文回来），小修、追问、已明确指出问题这类返工优先让它继续改——比重新派发省事得多，它知道上一轮为什么这么做。只有方向变了、或上一轮的实现整体不合格时，才重新派发一个干净的。

复用不等于放弃隔离：验收权始终在你手里，子 Agent 有没有上下文都不改变「它无权下结论」这件事。但要注意它的上下文会随复用累积——反复复用以致膨胀失控时，换一个新的重新派发。

**最终交付给用户。** 审查通过后，把结果汇报给用户：做了什么、交付物在哪、怎么用。用户只关心需求是否被满足，不关心你和子 Agent 之间怎么来回。中间过程不必细讲。

**停止时怎么说。** 话术不限，但必须让用户一眼看清：**卡在哪、需要什么、为什么停。**

- **已通过**：任务已通过审查，交付内容是什么。
- **需用户裁决**：争议点是什么、双方各自立场、需要用户定什么。
- **需求不清**：哪一处不明确，需要用户补什么。
- **子 Agent 无法交付**：它报的阻塞点是什么，你已经尝试过什么。

## 2. 并行派发多个子 Agent

子 Agent 之间互相不可见，也**不会有任何文件锁或自动串行**——平台不会替你避免冲突。两个子 Agent 同时改同一个文件，各自读到旧内容、各自写回，后写的会静默覆盖先写的：**不报错、不提示，改动凭空消失**。你在审查时也看不出来，两份汇报都符合各自的任务说明，错的只有合并后的结果。

所以：

- 能划清边界才并行：每个子 Agent 的改动文件必须**互不重叠**。
- 改动范围无法划清，或必然重叠（例如都要动同一个配置文件、都要跑同一个格式化命令）时，**改为串行**，一个完成后再派下一个。
- 并行派发后**不要直接采信汇总**。核对各自汇报的改动清单有无交集；一旦有交集，亲自查看文件实际状态，判断是否有改动被覆盖。
- 串行时注意依赖顺序：后一个子 Agent 需要前一个的产出才能干活，就必须等前一个返回并验证后再派。

**返工同样适用。** 需要同时返工多个子 Agent 时，按上面同样的规矩处理边界；不能划清就逐个返工。

## 3. 子 Agent 怎么做

- 严格按任务说明做事，不扩大范围。
- 顺手修掉明显问题可以，但要在汇报里写清楚。
- 可以补测试，且应该补在项目约定放测试的地方（各语言惯例不同，按项目现状走）。补不了要在汇报里说明原因。
- 可以运行编译、类型检查、lint、构建等检查，但**不运行测试套件**——那是你的活。若某条命令会连带跑测试且拆不开，就不要运行，并说明。
- 格式化由子 Agent 负责，按项目约定运行。
- 禁止以审查者口吻审查交付物，禁止说「通过」。

子 Agent 的汇报必须结构化，至少四条：

- **改动清单**：每处改了什么（位置 + 简述）。
- **测试情况**：补了什么、跑没跑、结果如何；没跑要说明。
- **未决事项 / 阻塞点**：没搞定的、拿不准的、需要审查者注意的。
- **自检声明**：跑过哪些非测试类检查，结果如何。

## 4. 流程

1. 你接收用户需求。用户是派发方：需求由用户提出，标准和约束由你补全，需求不清先问用户，不要靠猜。
2. 你起草任务说明并派发子 Agent。**一次只需一个就派一个；要派多个，先按第 2 节确认改动范围不重叠。**
3. 子 Agent 实现并汇报。
4. 你独立验证并按上面的三条判定。
5. 不通过则返工：**优先复用原子 Agent**，方向变了才重新派发，循环直到通过或需用户裁决。
6. 你把最终结果汇报给用户。

以上是默认节奏。用户可在任意一步介入引导，此时按用户最新要求调整后续步骤，必要时回到第 1 步重走。

你不得跳过审查直接宣称完成；子 Agent 不得自行宣布验收通过。
